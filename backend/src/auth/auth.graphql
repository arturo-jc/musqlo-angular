extend type Query {
  logIn(email: String!, password: String!, username: String): User
}

extend type Mutation {
  signUp(email: String!, password: String!, username: String): User
}

type User {
  id: String
  username: String
  email: String
}

