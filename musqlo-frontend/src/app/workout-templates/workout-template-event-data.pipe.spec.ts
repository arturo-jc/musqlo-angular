import { WorkoutTemplateEventDataPipe } from './workout-template-event-data.pipe';

describe('WorkoutTemplateEventDataPipe', () => {
  it('create an instance', () => {
    const pipe = new WorkoutTemplateEventDataPipe();
    expect(pipe).toBeTruthy();
  });
});
